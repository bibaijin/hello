appname: hello

build:
  base: golang:1.8
  prepare:
    version: 20170421
  script:
    - mkdir $GOPATH/src/github.com/bibaijin/tcp-reverse-proxy/
    - cp -rf . $GOPATH/src/github.com/bibaijin/tcp-reverse-proxy/
    - cd $GOPATH/src/github.com/bibaijin/tcp-reverse-proxy/ && go install

release:
  dest_base: registry.yxapp.xyz/centos:1.0.1
  copy:
    - src: $GOPATH/bin/hello
      dest: /lain/app/hello

web:
  cmd: /lain/app/hello
  port: 8080
  memory: 64M
  healthcheck: "/ping"
